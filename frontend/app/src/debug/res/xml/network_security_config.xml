<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only. Production traffic is HTTPS with a publicly trusted
     certificate (Caddy + Let's Encrypt on the VM), so no custom CA is needed.
     Plain HTTP is allowed to the emulator's host alias and localhost so a
     backend running on your machine (http://10.0.2.2:3000) still works. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
